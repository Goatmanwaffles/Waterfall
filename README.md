# DatabaseFinalProject

# Running
### Packages
##### For pip
`python3 -m venv venv`
`./venv/bin/activate`
`pip install -r requirements.txt`
##### For uv
Install `uv sync`
Run `uv run app.py`

### XAMPP
Run Xampp, we have seed data in the sql folder