# DatabaseFinalProject

# Running
### XAMPP
First, run Xampp, we have seed data in the sql folder
### Packages
##### For pip
```
python3 -m venv venv
./venv/bin/activate
pip install -r requirements.txt
```
##### For uv
```
uv sync
uv run app.py
```

